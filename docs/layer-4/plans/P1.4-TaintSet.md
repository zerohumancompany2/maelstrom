# Phase P1.4: TaintSet Operations

**Phase ID**: L4-P1.4  
**Title**: TaintSet Operations  
**Parent**: Phase P1 (Taint Engine Core)  
**Status**: ⚠️ PARTIAL (types exist, operations need testing)

## Parent Requirements

From `arch-v1.md L273` - Taint Sets:
1. Every Message, ContextBlock, and ApplicationContext value carries an immutable taint set
2. Taint sets are arrays of strings (e.g., `["PII", "SECRET", "INNER_ONLY", "USER_SUPPLIED"]`)

From `spec-extraction.md L36-58` - TaintSet type:
1. Add taint to set
2. Check if taint exists in set
3. Union two taint sets

## Dependencies

None (foundational phase, types already exist)

## Satisfied Lower-Layer Requirements

- Layer 2: TaintSet type exists in `pkg/security/types.go` (implementation-assessment.md L37)
- Layer 2: TaintMap type exists in `pkg/security/types.go` (implementation-assessment.md L38)

## Acceptance Criteria

1. TaintSet.Add adds taint to set without duplicates
2. TaintSet.Has returns true for existing taints, false for non-existent
3. TaintSet.Union combines two sets without duplicates
4. Operations on empty sets work correctly
5. Union with empty set returns original set unchanged

## Test Descriptions

### Test 1: TestTaintSet_AddHas

**Given**: An empty TaintSet  
**When**: Add("PII") is called, then Has("PII") is checked  
**Then**: Has("PII") returns true; Has("SECRET") returns false  
**Expected Result**: Add adds taint to set; Has correctly checks membership  
**Spec Reference**: arch-v1.md L273 (taint sets), spec-extraction.md L36-58 (TaintSet type)

### Test 2: TestTaintSet_Union

**Given**: Two TaintSets: set1 = `["PII", "SECRET"]`, set2 = `["SECRET", "TOOL_OUTPUT"]`  
**When**: Union is called with set1 and set2  
**Then**: Result is `["PII", "SECRET", "TOOL_OUTPUT"]` with no duplicates  
**Expected Result**: Union combines both sets; SECRET appears only once  
**Spec Reference**: arch-v1.md L273 (taint sets), L283 (taint propagation/merge)

### Test 3: TestTaintSet_EmptyOperations

**Given**: An empty TaintSet  
**When**: Various operations (Add, Has, Union) are called  
**Then**: Add works on empty set; Has returns false for all; Union with empty returns original  
**Expected Result**: Operations on empty set work correctly without errors  
**Spec Reference**: arch-v1.md L273 (taint sets), implementation-assessment.md L37-38

## Implementation Plan

### Files to Modify

1. `pkg/security/types.go` - Verify TaintSet operations exist and work correctly
2. `pkg/security/security_test.go` - Add tests for TaintSet operations

### Existing Types (from implementation-assessment.md L36-58)

```go
// pkg/security/types.go (already exists)
type TaintSet map[string]struct{}

func (ts TaintSet) Add(taint string)
func (ts TaintSet) Has(taint string) bool
func (ts TaintSet) Union(other TaintSet) TaintSet
```

### Implementation Order

1. Verify TaintSet type and methods exist in `pkg/security/types.go`
2. Write Test 1 → RED → Fix/Add Add/Has methods → GREEN → commit
3. Write Test 2 → RED → Fix/Add Union method → GREEN → commit
4. Write Test 3 → RED → Verify empty set handling → GREEN → commit

## Commit Plan

| Test | Commit Message |
|------|----------------|
| Test 1 | `feat(layer-4/taint): add TaintSet Add and Has operations` |
| Test 2 | `feat(layer-4/taint): add TaintSet Union operation` |
| Test 3 | `feat(layer-4/taint): add TaintSet empty operations handling` |

## Deliverables

- 3 commits (1:1 test-to-commit ratio)
- All tests passing
- Files modified: `pkg/security/types.go`, `pkg/security/security_test.go`

---

*Document generated: 2026-03-09*  
*Based on: arch-v1.md v1.3, phase-breakdown.md, implementation-assessment.md, spec-extraction.md*
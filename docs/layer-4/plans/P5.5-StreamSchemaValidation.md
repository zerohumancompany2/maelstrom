# Phase P5.5: StreamSchemaValidation

**Phase ID**: L4-P5.5  
**Title**: StreamSchemaValidation - Schema Validation for Outgoing Chunks  
**Parent**: Phase P5 (DMZ Sanitizers)  
**Status**: ❌ PENDING

## Parent Requirements

From `arch-v1.md L268` - DMZ Sanitization:
- outputs are sanitized (PII redaction, length limits, schema validation)

From `arch-v1.md L298` - Outgoing Chunk Processing:
- Every outgoing chunk passes through DMZ sanitizers (PII redaction, length caps, schema validation, inner-data stripping)

## Dependencies

- Phase P5.1: PII Redaction (schema validation requires sanitized data structure)
- Phase P5.3: Length Caps (length validation complements schema validation)

## Satisfied Lower-Layer Requirements

- Layer 3: Mail system with schema-constrained message types (arch-v1.md L170, L50)
- Layer 2: sys:security service skeleton with validation hooks (implementation-assessment.md L58-73)
- Layer 0: ApplicationContext with structured data interfaces (arch-v1.md L383-387)

## Acceptance Criteria

1. Validate outgoing chunks against expected schema before DMZ exit
2. Reject chunks with missing required fields or invalid types
3. Return structured validation errors for debugging
4. Preserve original chunk on validation failure (immutable validation)

## Test Descriptions

### Test 1: TestStreamSchemaValidation_Validate_ValidChunk

**Given**: An outgoing chunk with all required fields and correct types  
**When**: Validate is called with the chunk against the schema  
**Then**: Validation returns nil error  
**Expected Result**: Chunk passes schema validation, no modifications to original chunk  
**Spec Reference**: arch-v1.md L268 (schema validation), L298 (outgoing chunk sanitization)

### Test 2: TestStreamSchemaValidation_Validate_InvalidChunk

**Given**: An outgoing chunk missing required field "source" with invalid type for "data" field  
**When**: Validate is called with the chunk against the schema  
**Then**: Validation returns error listing both validation failures  
**Expected Result**: Structured error contains missing field error and type error, original chunk unchanged  
**Spec Reference**: arch-v1.md L268 (schema validation), L298 (outgoing chunk sanitization)

## Implementation Plan

### Files to Modify

1. `pkg/security/schema.go` - Create Validate function and schema definitions
2. `pkg/security/schema_test.go` - Add tests

### Stubs Needed

```go
// pkg/security/schema.go
func Validate(chunk any, schema Schema) error
```

### Implementation Order

1. Create `pkg/security/schema.go` with stub implementation
2. Write Test 1 → RED → Implement valid chunk validation → GREEN → commit
3. Write Test 2 → RED → Implement error reporting for invalid chunks → GREEN → commit

## Commit Plan

| Test | Commit Message |
|------|----------------|
| Test 1 | `feat(layer-4/dmz): add schema validation for valid chunks` |
| Test 2 | `feat(layer-4/dmz): add schema validation error reporting` |

## Deliverables

- 2 commits (1:1 test-to-commit ratio)
- All tests passing
- Files modified: `pkg/security/schema.go`, `pkg/security/schema_test.go`

---

*Document generated: 2026-03-09*  
*Based on: arch-v1.md v1.3, phase-breakdown.md, implementation-assessment.md*
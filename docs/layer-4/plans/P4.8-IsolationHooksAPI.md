# Phase P4.8: DevOps Isolation Hooks API

**Phase ID**: L4-P4.8  
**Title**: DevOps Isolation Hooks API  
**Parent**: Phase P4 (DevOps & CI/CD Integration)  
**Status**: ❌ PENDING

## Parent Requirements

From `arch-v1.md L400` - Hook for hot-reload:
- Provide `replaceDefinition` hook for hot-reload (running instance decides acceptance)

From `arch-v1.md L516-570` - Isolation levels for tools:
- `isolation: strict` - each tool in own namespace
- `isolation: process` - process-level isolation
- `isolation: container` - container-based isolation

From `arch-v1.md L660` - Webhook support:
- `webhook` - HTTP POST endpoints for external integration

## Dependencies

- Phase P4.1 (CI/CD Pipeline Interface)
- Phase P4.2 (Artifact Registry Integration)
- Phase P1 (Taint Engine Core) - for hook taint propagation

## Satisfied Lower-Layer Requirements

- Layer 3: Mail system with webhook support (arch-v1.md L660)
- Layer 2: sys:security service for isolation enforcement (implementation-assessment.md L58-73)
- Layer 0: ApplicationContext with namespace isolation (arch-v1.md L43)

## Acceptance Criteria

1. IsolationHooks API provides `replaceDefinition` hook for hot-reload scenarios
2. IsolationHooks API provides `onToolExecution` hook for pre/post-execution logic
3. Hooks respect isolation levels (strict, process, container)
4. Hooks propagate taints from input to output

## Test Descriptions

### Test 1: TestIsolationHooks_ReplaceDefinition_Accept

**Given**: A running tool instance with isolation level `strict`  
**When**: `replaceDefinition` hook is called with new definition that maintains same signature  
**Then**: Running instance accepts the new definition and reloads without interruption  
**Expected Result**: Tool continues operation with new definition, isolation boundary maintained  
**Spec Reference**: arch-v1.md L400 (replaceDefinition hook), L516 (strict isolation)

### Test 2: TestIsolationHooks_OnToolExecution_TaintPropagation

**Given**: A tool execution hook with input data tainted `["USER_SUPPLIED"]`  
**When**: `onToolExecution` hook processes the input and produces output  
**Then**: Output data carries taint `["USER_SUPPLIED", "TOOL_OUTPUT"]`  
**Expected Result**: Taints are preserved and augmented through hook execution chain  
**Spec Reference**: arch-v1.md L283 (taint propagation), L561 (container isolation)

## Implementation Plan

### Files to Modify

1. `pkg/devops/hooks.go` - Create IsolationHooks API
2. `pkg/devops/hooks_test.go` - Add tests

### Stubs Needed

```go
// pkg/devops/hooks.go
type IsolationHooks struct {
    replaceDefinition func(oldDef, newDef *ToolDefinition) error
    onToolExecution   func(input any) (any, error)
}

func (h *IsolationHooks) ReplaceDefinition(oldDef, newDef *ToolDefinition) error
func (h *IsolationHooks) OnToolExecution(input any) (any, error)
```

### Implementation Order

1. Create `pkg/devops/hooks.go` with stub implementations
2. Write Test 1 → RED → Implement replaceDefinition logic → GREEN → commit
3. Write Test 2 → RED → Implement onToolExecution with taint propagation → GREEN → commit

## Commit Plan

| Test | Commit Message |
|------|----------------|
| Test 1 | `feat(layer-4/devops): add replaceDefinition hook for hot-reload` |
| Test 2 | `feat(layer-4/devops): add onToolExecution hook with taint propagation` |

## Deliverables

- 2 commits (1:1 test-to-commit ratio)
- All tests passing
- Files modified: `pkg/devops/hooks.go`, `pkg/devops/hooks_test.go`

---

*Document generated: 2026-03-09*  
*Based on: arch-v1.md v1.3, phase-breakdown.md, implementation-assessment.md*
# Phase P5.6: sysHumanGatewaySanitization

**Phase ID**: L4-P5.6  
**Title**: sys:human-gateway Sanitization for Human-in-the-Loop Chat  
**Parent**: Phase P5 (DMZ Sanitizers)  
**Status**: ❌ PENDING

## Parent Requirements

From `arch-v1.md L731` - Human Gateway Chat Session:
- Session receives read-only snapshot of Agent's current ContextMap + last N Messages (sanitized by boundary rules)

From `arch-v1.md L2348` - sys:human-gateway Definition:
- Hot-reloadable service providing chat interface for human-in-the-loop with running agents at `/chat/{agentId}`

From `arch-v1.md L268` - DMZ Sanitization:
- outputs are sanitized (PII redaction, length limits, schema validation)
- ContextMap blocks marked with forbidden taints are stripped before any LLM call

## Dependencies

- Phase P5.1: PII Redaction (sanitization requires PII removal)
- Phase P5.3: ContextPreparation (sanitization operates on prepared context)
- Phase P5.4: StreamLengthCaps (length limits apply to chat output)
- Phase P5.5: StreamSchemaValidation (schema validation complements sanitization)

## Satisfied Lower-Layer Requirements

- Layer 3: Mail system with taint-aware message types (arch-v1.md L170, L605)
- Layer 2: sys:security service skeleton with boundary enforcement (implementation-assessment.md L58-73)
- Layer 0: ContextMap with taint-aware block access (arch-v1.md L383-387)

## Acceptance Criteria

1. Sanitize ContextMap blocks for human-gateway chat session (strip forbidden taints)
2. Sanitize message history for human-gateway display (apply boundary rules)
3. Return read-only snapshot of sanitized data to human operator
4. Reject access to inner-boundary data (return sanitized views only)

## Test Descriptions

### Test 1: TestHumanGatewaySanitization_SanitizeContextMap_StripsForbiddenTaints

**Given**: A ContextMap with blocks containing taints `["SECRET", "INNER_ONLY", "PII"]`  
**When**: SanitizeContextMap is called for human-gateway chat session  
**Then**: Returned ContextMap snapshot excludes blocks with forbidden taints (`SECRET`, `INNER_ONLY`) and redacts PII  
**Expected Result**: Human operator sees only boundary-appropriate data, forbidden taints stripped  
**Spec Reference**: arch-v1.md L731 (sanitized by boundary rules), L268 (forbidden taints stripped)

### Test 2: TestHumanGatewaySanitization_SanitizeMessageHistory_BoundaryEnforcement

**Given**: A message history with messages from inner-boundary tools containing `["TOOL_OUTPUT", "SECRET"]` taints  
**When**: SanitizeMessageHistory is called for human-gateway display  
**Then**: Messages with forbidden taints are redacted or replaced with sanitized placeholder  
**Expected Result**: Human operator sees sanitized message history, inner-boundary data hidden  
**Spec Reference**: arch-v1.md L731 (sanitized by boundary rules), L1577 (sanitized views only)

## Implementation Plan

### Files to Modify

1. `pkg/security/human_gateway.go` - Create sanitization functions for human-gateway
2. `pkg/security/human_gateway_test.go` - Add tests

### Stubs Needed

```go
// pkg/security/human_gateway.go
func SanitizeContextMap(ctx ContextMap, boundary Boundary) (ContextMap, error)
func SanitizeMessageHistory(messages []Message, boundary Boundary) ([]Message, error)
```

### Implementation Order

1. Create `pkg/security/human_gateway.go` with stub implementations
2. Write Test 1 → RED → Implement ContextMap sanitization → GREEN → commit
3. Write Test 2 → RED → Implement message history sanitization → GREEN → commit

## Commit Plan

| Test | Commit Message |
|------|----------------|
| Test 1 | `feat(layer-4/human-gateway): add ContextMap sanitization for chat sessions` |
| Test 2 | `feat(layer-4/human-gateway): add message history sanitization with boundary enforcement` |

## Deliverables

- 2 commits (1:1 test-to-commit ratio)
- All tests passing
- Files modified: `pkg/security/human_gateway.go`, `pkg/security/human_gateway_test.go`

---

*Document generated: 2026-03-09*  
*Based on: arch-v1.md v1.3, phase-breakdown.md, implementation-assessment.md*
# Phase P3.1: PersistenceService Core

**Phase ID**: L5-P3.1  
**Title**: PersistenceService Core  
**Parent**: Phase P3 (PersistenceService)  
**Status**: ❌ PENDING

## Parent Requirements

From `arch-v1.md L468, L486` - PersistenceService:
1. PersistenceService must return ID "sys:persistence"
2. PersistenceService must create snapshots
3. PersistenceService must restore from snapshots
4. PersistenceService must append events
5. PersistenceService must query events

From `arch-v1.md L462-474` (Hot-Reloadable Services):
- Platform services SHALL be hot-reloadable without manual intervention

## Dependencies

- None (independent phase)

## Satisfied Lower-Layer Requirements

- Layer 4: Security infrastructure complete (taint engine, boundary enforcement)
- Layer 3: Mail system complete (publish/subscribe, routing, inbox)
- Layer 2: Service registry complete (Service interface, ServiceRegistry)
- Layer 1: ChartRegistry exists (YAML loading infrastructure)
- Layer 0: Statechart Engine complete

## Acceptance Criteria

1. PersistenceService.ID() returns "sys:persistence"
2. PersistenceService can create snapshots with runtime state, taints, and context
3. PersistenceService can restore from snapshots and return new RuntimeID
4. PersistenceService can append events with timestamps to event log
5. PersistenceService can query events with filters in chronological order

## Test Descriptions

### Test 1: TestPersistenceService_ID

**Given**: A newly created PersistenceService instance  
**When**: ID() method is called  
**Then**: Returns "sys:persistence"  
**Expected Result**: ID is immutable and matches well-known service ID  
**Spec Reference**: arch-v1.md L468 (sys:persistence service), L477-480 (Platform Service Contract)

### Test 2: TestPersistenceService_Snapshot

**Given**: A PersistenceService instance and a running runtime with state  
**When**: Snapshot() is called with runtime ID  
**Then**: Snapshot created with runtime state, taints, and context  
**Expected Result**: Snapshot includes full runtime state, taints attached, context preserved  
**Spec Reference**: arch-v1.md L468 (Snapshots, event sourcing, version migration), L486 (snapshot(runtimeId))

### Test 3: TestPersistenceService_Restore

**Given**: A PersistenceService instance and a saved snapshot  
**When**: Restore() is called with snapshot ID  
**Then**: Runtime restored from snapshot with new RuntimeID  
**Expected Result**: New RuntimeID returned, state restored correctly, taints preserved  
**Spec Reference**: arch-v1.md L468 (Snapshots, event sourcing, version migration), L486 (restore(snapshotId))

### Test 4: TestPersistenceService_AppendEvent

**Given**: A PersistenceService instance and an event to record  
**When**: AppendEvent() is called with event  
**Then**: Event appended to event log with timestamp  
**Expected Result**: Event stored with timestamp, event associated with runtime ID  
**Spec Reference**: arch-v1.md L468 (event sourcing)

### Test 5: TestPersistenceService_QueryEvents

**Given**: A PersistenceService instance with events in the log  
**When**: GetEvents() is called with filters  
**Then**: Events returned in chronological order matching filters  
**Expected Result**: Events filtered correctly, chronological ordering maintained  
**Spec Reference**: arch-v1.md L468 (event sourcing)

## Implementation Plan

### Files to Modify

1. `pkg/services/persistence/service.go` - Complete PersistenceService implementation
2. `pkg/services/persistence/service_test.go` - Add tests
3. `pkg/services/persistence/storage.go` - Storage backend interface

### Stubs Needed

```go
// pkg/services/persistence/service.go
func NewPersistenceService() *PersistenceService
func (s *PersistenceService) ID() string
func (s *PersistenceService) HandleMail(mail Mail) error
func (s *PersistenceService) Snapshot(runtimeId string) (Snapshot, error)
func (s *PersistenceService) Restore(snapshotId string) (statechart.RuntimeID, error)
func (s *PersistenceService) AppendEvent(event Event) error
func (s *PersistenceService) GetEvents(filters EventFilters) ([]Event, error)
```

### Implementation Order

1. Create `pkg/services/persistence/service.go` with stub implementation
2. Create `pkg/services/persistence/storage.go` with StorageBackend interface
3. Write Test 1 → RED → Implement ID() → GREEN → commit
4. Write Test 2 → RED → Implement Snapshot → GREEN → commit
5. Write Test 3 → RED → Implement Restore → GREEN → commit
6. Write Test 4 → RED → Implement AppendEvent → GREEN → commit
7. Write Test 5 → RED → Implement GetEvents → GREEN → commit

## Commit Plan

| Test | Commit Message |
|------|----------------|
| Test 1 | `feat(layer-5/persistence): add PersistenceService ID method` |
| Test 2 | `feat(layer-5/persistence): add PersistenceService Snapshot` |
| Test 3 | `feat(layer-5/persistence): add PersistenceService Restore` |
| Test 4 | `feat(layer-5/persistence): add PersistenceService AppendEvent` |
| Test 5 | `feat(layer-5/persistence): add PersistenceService QueryEvents` |

## Deliverables

- 5 commits (1:1 test-to-commit ratio)
- All tests passing
- Files modified: `pkg/services/persistence/service.go`, `pkg/services/persistence/service_test.go`, `pkg/services/persistence/storage.go`

---

*Document generated: 2026-03-09*  
*Based on: arch-v1.md v1.3, phase-breakdown.md, implementation-assessment.md*
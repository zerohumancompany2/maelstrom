# Phase P1.1: GatewayService Core

**Phase ID**: L5-P1.1  
**Title**: GatewayService Core  
**Parent**: Phase P1 (GatewayService Core)  
**Status**: ❌ PENDING

## Parent Requirements

From `arch-v1.md L466, L657-671` - GatewayService:
1. GatewayService must return ID "sys:gateway"
2. GatewayService must register channel adapters
3. GatewayService must normalize inbound messages to mail_received
4. GatewayService must normalize outbound messages to channel-specific format

From `arch-v1.md L462-474` (Hot-Reloadable Services):
- Platform services SHALL be hot-reloadable without manual intervention

## Dependencies

None (foundational phase)

## Satisfied Lower-Layer Requirements

- Layer 4: Security infrastructure complete (taint engine, boundary enforcement)
- Layer 3: Mail system complete (publish/subscribe, routing, inbox)
- Layer 2: Service registry complete (Service interface, ServiceRegistry)
- Layer 1: ChartRegistry exists (YAML loading infrastructure)
- Layer 0: Statechart Engine complete

## Acceptance Criteria

1. GatewayService.ID() returns "sys:gateway"
2. GatewayService can register channel adapters by name
3. Inbound traffic normalized to mail_received with adapter metadata preserved
4. Outbound mail_send normalized to channel-specific format with boundary enforcement

## Test Descriptions

### Test 1: TestGatewayService_ID

**Given**: A newly created GatewayService instance  
**When**: ID() method is called  
**Then**: Returns "sys:gateway"  
**Expected Result**: ID is immutable and matches well-known service ID  
**Spec Reference**: arch-v1.md L466 (sys:gateway service), L477-480 (Platform Service Contract)

### Test 2: TestGatewayService_RegisterAdapter

**Given**: A GatewayService instance and a ChannelAdapter implementation  
**When**: RegisterAdapter is called with a unique adapter name  
**Then**: Adapter is registered and retrievable via GetAdapter  
**Expected Result**: Duplicate registration returns error, adapters stored correctly  
**Spec Reference**: arch-v1.md L659-666 (Channel Adapters), L466 (GatewayService)

### Test 3: TestGatewayService_NormalizeInbound

**Given**: An inbound message from a channel adapter with adapter-specific metadata  
**When**: NormalizeInbound is called on the message  
**Then**: Message normalized to mail_received type with metadata preserved in Mail.Metadata  
**Expected Result**: Adapter-specific fields preserved, standard mail_received format produced  
**Spec Reference**: arch-v1.md L670-671 (normalize inbound to mail_received)

### Test 4: TestGatewayService_NormalizeOutbound

**Given**: An outbound mail_send message with boundary-enforced content  
**When**: NormalizeOutbound is called for a specific channel adapter  
**Then**: Message converted to channel-specific format  
**Expected Result**: Boundary enforcement applied, channel format produced correctly  
**Spec Reference**: arch-v1.md L671 (normalize outbound to channel format), L261-270 (Boundary Model)

## Implementation Plan

### Files to Modify

1. `pkg/services/gateway/service.go` - Complete GatewayService implementation
2. `pkg/services/gateway/service_test.go` - Add tests

### Stubs Needed

```go
// pkg/services/gateway/service.go
func NewGatewayService() *GatewayService
func (s *GatewayService) ID() string
func (s *GatewayService) HandleMail(mail Mail) error
func (s *GatewayService) RegisterAdapter(name string, adapter ChannelAdapter) error
func (s *GatewayService) GetOpenAPI() *OpenAPISpec
```

### Implementation Order

1. Create `pkg/services/gateway/service.go` with stub implementation
2. Write Test 1 → RED → Implement ID() → GREEN → commit
3. Write Test 2 → RED → Implement RegisterAdapter → GREEN → commit
4. Write Test 3 → RED → Implement NormalizeInbound → GREEN → commit
5. Write Test 4 → RED → Implement NormalizeOutbound → GREEN → commit

## Commit Plan

| Test | Commit Message |
|------|----------------|
| Test 1 | `feat(layer-5/gateway): add GatewayService ID method` |
| Test 2 | `feat(layer-5/gateway): add GatewayService RegisterAdapter` |
| Test 3 | `feat(layer-5/gateway): add GatewayService NormalizeInbound` |
| Test 4 | `feat(layer-5/gateway): add GatewayService NormalizeOutbound` |

## Deliverables

- 4 commits (1:1 test-to-commit ratio)
- All tests passing
- Files modified: `pkg/services/gateway/service.go`, `pkg/services/gateway/service_test.go`

---

*Document generated: 2026-03-09*  
*Based on: arch-v1.md v1.3, phase-breakdown.md, implementation-assessment.md*
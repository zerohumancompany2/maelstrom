# Phase P1.2: GatewayChannelAdapters

**Phase ID**: L5-P1.2  
**Title**: GatewayChannelAdapters  
**Parent**: Phase P1 (GatewayService Core)  
**Status**: ❌ PENDING

## Parent Requirements

From `arch-v1.md L659-666` - Channel Adapters:
1. webhook adapter for HTTP POST endpoints
2. websocket adapter for full bidirectional communication
3. sse adapter for Server-Sent Events (firewall-friendly)
4. pubsub adapter for message queue integration
5. smtp adapter for email
6. platform adapters (slack, whatsapp, telegram)

From `arch-v1.md L462-474` (Hot-Reloadable Services):
- Platform services SHALL be hot-reloadable without manual intervention

## Dependencies

- P1.1: GatewayService Core (ChannelAdapter interface, adapter registration)

## Satisfied Lower-Layer Requirements

- Layer 4: Security infrastructure complete (taint engine, boundary enforcement)
- Layer 3: Mail system complete (publish/subscribe, routing, inbox)
- Layer 2: Service registry complete (Service interface, ServiceRegistry)
- Layer 1: ChartRegistry exists (YAML loading infrastructure)
- Layer 0: Statechart Engine complete

## Acceptance Criteria

1. Webhook adapter normalizes HTTP POST to mail_received
2. WebSocket adapter supports bidirectional message flow with connection state tracking
3. SSE adapter formats Server-Sent Events correctly on single HTTP connection
4. PubSub adapter integrates with message queue with topic-based routing
5. SMTP adapter formats email correctly with SMTP protocol compliance
6. Platform adapter (Slack) supports webhook and bot message formats

## Test Descriptions

### Test 1: TestWebhookAdapter_NormalizeInbound

**Given**: An HTTP POST request to a webhook endpoint with JSON body  
**When**: NormalizeInbound is called on the request  
**Then**: Request normalized to mail_received type with body preserved in Mail.Content  
**Expected Result**: HTTP POST converted to standard mail format, request body intact  
**Spec Reference**: arch-v1.md L659-666 (Channel Adapters), L670-671 (normalize inbound to mail_received)

### Test 2: TestWebSocketAdapter_Bidirectional

**Given**: A WebSocket connection established with the adapter  
**When**: Messages are sent and received over the connection  
**Then**: Bidirectional message flow works with connection state tracked  
**Expected Result**: Inbound messages normalized to mail_received, outbound messages sent correctly  
**Spec Reference**: arch-v1.md L661 (websocket - Full bidirectional), L670-671 (normalize inbound/outbound)

### Test 3: TestSSEAdapter_FirewallFriendly

**Given**: An SSE connection established to the adapter  
**When**: Server-sent events are pushed to the client  
**Then**: Events formatted correctly on single HTTP connection  
**Expected Result**: SSE event stream maintained, firewall-friendly single connection  
**Spec Reference**: arch-v1.md L662 (sse - Server-Sent Events firewall-friendly), L670-671 (normalize outbound)

### Test 4: TestPubSubAdapter_MessageQueue

**Given**: A PubSub adapter configured with a message queue topic  
**When**: Messages are published and subscribed on the topic  
**Then**: Message queue integration works with topic-based routing  
**Expected Result**: Messages delivered to correct topics, routing functional  
**Spec Reference**: arch-v1.md L663 (pubsub - Message queue integration), L670-671 (normalize inbound/outbound)

### Test 5: TestSMTPAdapter_Email

**Given**: An SMTP adapter configured with email server settings  
**When**: An email is sent through the adapter  
**Then**: Email formatted correctly with SMTP protocol compliance  
**Expected Result**: Email sent with proper headers and SMTP protocol  
**Spec Reference**: arch-v1.md L664 (smtp - Email), L670-671 (normalize outbound)

### Test 6: TestPlatformAdapter_Slack

**Given**: A Slack platform adapter configured with bot token  
**When**: Messages are sent and received via Slack API  
**Then**: Slack API integration works with webhook and bot message formats  
**Expected Result**: Slack messages normalized correctly, both formats supported  
**Spec Reference**: arch-v1.md L665 (slack, whatsapp, telegram - Messaging platforms), L670-671 (normalize inbound/outbound)

## Implementation Plan

### Files to Create

1. `pkg/services/gateway/adapters/webhook.go` - Webhook adapter implementation
2. `pkg/services/gateway/adapters/websocket.go` - WebSocket adapter implementation
3. `pkg/services/gateway/adapters/sse.go` - SSE adapter implementation
4. `pkg/services/gateway/adapters/pubsub.go` - PubSub adapter implementation
5. `pkg/services/gateway/adapters/smtp.go` - SMTP adapter implementation
6. `pkg/services/gateway/adapters/platform.go` - Slack, WhatsApp, Telegram adapters

### Stubs Needed

```go
// pkg/services/gateway/adapters/adapters.go
type ChannelAdapter interface {
    NormalizeInbound(mail Mail) (Mail, error)
    NormalizeOutbound(mail Mail) (any, error)
    Start() error
    Stop() error
}

// pkg/services/gateway/adapters/webhook.go
func NewWebhookAdapter(endpoint string) *WebhookAdapter

// pkg/services/gateway/adapters/websocket.go
func NewWebSocketAdapter(address string) *WebSocketAdapter

// pkg/services/gateway/adapters/sse.go
func NewSSEAdapter(address string) *SSEAdapter

// pkg/services/gateway/adapters/pubsub.go
func NewPubSubAdapter(topic string) *PubSubAdapter

// pkg/services/gateway/adapters/smtp.go
func NewSMTPAdapter(config SMTPConfig) *SMTPAdapter

// pkg/services/gateway/adapters/platform.go
func NewSlackAdapter(token string) *SlackAdapter
func NewWhatsAppAdapter(config WhatsAppConfig) *WhatsAppAdapter
func NewTelegramAdapter(token string) *TelegramAdapter
```

### Implementation Order

1. Create `pkg/services/gateway/adapters/` directory with adapter interface
2. Write Test 1 → RED → Implement WebhookAdapter → GREEN → commit
3. Write Test 2 → RED → Implement WebSocketAdapter → GREEN → commit
4. Write Test 3 → RED → Implement SSEAdapter → GREEN → commit
5. Write Test 4 → RED → Implement PubSubAdapter → GREEN → commit
6. Write Test 5 → RED → Implement SMTPAdapter → GREEN → commit
7. Write Test 6 → RED → Implement SlackAdapter → GREEN → commit

## Commit Plan

| Test | Commit Message |
|------|----------------|
| Test 1 | `feat(layer-5/gateway): add WebhookAdapter for HTTP POST endpoints` |
| Test 2 | `feat(layer-5/gateway): add WebSocketAdapter for bidirectional communication` |
| Test 3 | `feat(layer-5/gateway): add SSEAdapter for firewall-friendly events` |
| Test 4 | `feat(layer-5/gateway): add PubSubAdapter for message queue integration` |
| Test 5 | `feat(layer-5/gateway): add SMTPAdapter for email` |
| Test 6 | `feat(layer-5/gateway): add SlackAdapter for messaging platforms` |

## Deliverables

- 6 commits (1:1 test-to-commit ratio)
- All tests passing
- Files created: `pkg/services/gateway/adapters/{webhook,websocket,sse,pubsub,smtp,platform}.go`
- Adapter test files: `pkg/services/gateway/adapters/*_test.go`

---

*Document generated: 2026-03-09*  
*Based on: arch-v1.md v1.3, phase-breakdown.md, implementation-assessment.md*
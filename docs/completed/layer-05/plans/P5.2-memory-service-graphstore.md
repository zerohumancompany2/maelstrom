# Phase P5.2: MemoryService GraphStore

**Parent**: Phase P5 (MemoryService)  
**Spec References**: arch-v1.md L470, L489 (MemoryService), L462-474 (Hot-Reloadable Services)  
**Status**: ❌ PENDING

---

## Overview

| Metric | Value |
|--------|-------|
| Tests | 4 |
| Commits | 4 (1:1 ratio) |
| Dependencies | P5.1 |
| Branch | `feat/layer-05-memory-graphstore` |

---

## Requirements

From `arch-v1.md L470` - MemoryService:

> - **sys:memory** — Long-term memory (vector/graph stores) for ContextMap injection

From `arch-v1.md L489` - MemoryService Contract:

> - **sys:memory**: `query(vector, topK, boundaryFilter) → MessageSlice`

From `arch-v1.md L462-474` - Hot-Reloadable Services:

> All platform services SHALL be hot-reloadable without manual intervention

### Functional Requirements

1. GraphStore must add edges between nodes with properties
2. GraphStore must query patterns and return matching nodes
3. GraphStore must traverse relationships from a node
4. GraphStore queries must respect boundary filters

---

## Implementation Details

### Files to Create/Modify

| File | Action |
|------|--------|
| `pkg/services/memory/graph.go` | Create - GraphStore implementation |
| `pkg/services/memory/service_test.go` | Modify - Add graph tests |

### Functions to Implement

```go
type GraphStore interface {
    AddEdge(from, to string, properties any) error
    Query(pattern GraphPattern) ([]GraphNode, error)
}

type GraphPattern struct {
    From         string
    To           string
    Relationship string
    Properties   map[string]any
}

type GraphNode struct {
    ID         string
    Properties map[string]any
    Edges      []GraphEdge
}

type GraphEdge struct {
    From       string
    To         string
    Type       string
    Properties map[string]any
}
```

---

## Tests to Write (4 tests, 4 commits)

### Test 1: TestMemoryService_AddEdge

```go
func TestMemoryService_AddEdge(t *testing.T)
```

**Acceptance Criteria**:
- Edge added between nodes
- Properties stored with edge
- Duplicate edge handling (overwrite or error)

**Commit Message**: `feat(layer-05/memory): TestMemoryService_AddEdge`

---

### Test 2: TestMemoryService_QueryPattern

```go
func TestMemoryService_QueryPattern(t *testing.T)
```

**Acceptance Criteria**:
- Pattern query returns matching nodes
- Relationships traversed correctly
- Property filtering works

**Commit Message**: `feat(layer-05/memory): TestMemoryService_QueryPattern`

---

### Test 3: TestMemoryService_TraverseRelationships

```go
func TestMemoryService_TraverseRelationships(t *testing.T)
```

**Acceptance Criteria**:
- Relationships traversed from node
- All connected nodes returned
- Multi-hop traversal supported

**Commit Message**: `feat(layer-05/memory): TestMemoryService_TraverseRelationships`

---

### Test 4: TestMemoryService_BoundaryFilteredQuery

```go
func TestMemoryService_BoundaryFilteredQuery(t *testing.T)
```

**Acceptance Criteria**:
- Query respects boundary filter
- Forbidden taints excluded from results
- Nodes with restricted access filtered out

**Commit Message**: `feat(layer-05/memory): TestMemoryService_BoundaryFilteredQuery`

---

## Execution Plan

1. `git checkout -b feat/layer-05-memory-graphstore`
2. Stub all public symbols (zero implementation, only signatures)
3. Write Test 1 (TestMemoryService_AddEdge) → Run → RED → Implement → GREEN → Commit
4. Write Test 2 (TestMemoryService_QueryPattern) → Run → RED → Implement → GREEN → Commit
5. Write Test 3 (TestMemoryService_TraverseRelationships) → Run → RED → Implement → GREEN → Commit
6. Write Test 4 (TestMemoryService_BoundaryFilteredQuery) → Run → RED → Implement → GREEN → Commit
7. Run full test suite → Verify all green
8. Open PR

---

## Dependencies

- P5.1 (MemoryService VectorStore) - Must be completed first

## Success Criteria

- All 4 tests pass
- 100% test coverage on modified files
- 4 commits with 1:1 test-to-commit ratio
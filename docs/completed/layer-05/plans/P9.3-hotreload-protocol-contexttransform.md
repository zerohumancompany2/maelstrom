# Phase P9.3: HotReloadProtocol ContextTransform

**Parent**: Phase P9 (HotReloadProtocol)  
**Spec References**: arch-v1.md L882-885 (Context Transform), L462-474 (Hot-Reloadable Services)  
**Status**: ❌ PENDING

---

## Overview

- **Tests**: 3
- **Commits**: 3 (1:1 ratio)
- **Dependencies**: P9.2 (HotReloadProtocol History)
- **Files**: 1 (lifecycle service.go modifications)

---

## Requirements

From `arch-v1.md L882-885` - Context Transform:

> **L882**: `**Context Transform**:`
> **L883**: `- Go template with access to oldContext, newVersion, contextVersion`
> **L884**: `- Executed on version change; failure → cleanStart fallback`
> **L885**: `- Registry SHALL validate template syntax at load time`

From `arch-v1.md L462-474` - Hot-Reloadable Services:

> **L462**: `### 7.2 Hot-Reloadable Platform Services`
> **L463**: 
> **L464**: `All platform services are themselves Charts loaded by the ChartRegistry, running on the Statechart Engine.`

1. Go template with access to oldContext, newVersion, contextVersion
2. Executed on version change; failure → cleanStart fallback
3. Registry SHALL validate template syntax at load time

---

## Implementation Details

**Files to create/modify**:

- `pkg/services/lifecycle/service.go` - Add context transform
- `pkg/services/lifecycle/service_test.go` - Add transform tests

**Functions to implement**:

```go
func (s *LifecycleService) applyContextTransform(oldContext any, newVersion string, template string) (any, error)
func validateTransformTemplate(template string) error
```

---

## Tests to Write (3 tests, 3 commits)

### Test 1: TestHotReload_ContextTransform

```go
func TestHotReload_ContextTransform(t *testing.T)
```

**Acceptance Criteria**:
- Template executed with oldContext
- newVersion accessible in template
- contextVersion accessible in template
- New context returned
- Template variables properly substituted

**Commit Message**: `feat(layer-5/lifecycle): apply context transform template`

---

### Test 2: TestHotReload_TransformFailureFallback

```go
func TestHotReload_TransformFailureFallback(t *testing.T)
```

**Acceptance Criteria**:
- Transform failure detected
- Fallback to cleanStart
- Error logged for failure
- cleanStart invoked on failure
- Runtime still spawned with clean context

**Commit Message**: `feat(layer-5/lifecycle): fallback to cleanStart on transform failure`

---

### Test 3: TestHotReload_TemplateValidation

```go
func TestHotReload_TemplateValidation(t *testing.T)
```

**Acceptance Criteria**:
- Template syntax validated at load time
- Invalid template rejected
- Error returned for invalid syntax
- Valid template accepted
- Go template parsing used

**Commit Message**: `feat(layer-5/lifecycle): validate transform template syntax`

---

## Execution Order

```
P9.3: HotReloadProtocol ContextTransform
├── Test 1: TestHotReload_ContextTransform
├── Test 2: TestHotReload_TransformFailureFallback
└── Test 3: TestHotReload_TemplateValidation
```

---

## TDD Workflow

1. `git checkout -b feat/layer5-hotreload-transform`
2. Stub `applyContextTransform`, `validateTransformTemplate` in lifecycle/service.go
3. Write `TestHotReload_ContextTransform` → RED
4. Implement minimal template execution → GREEN
5. `git commit -m "feat(layer-5/lifecycle): apply context transform template"`
6. Repeat for tests 2-3

---

## Dependencies

- **P9.2**: HotReloadProtocol History must be complete

---

## Next Phase

After P9.3 completes, proceed to **P10.1: PlatformServiceYAML Schema**
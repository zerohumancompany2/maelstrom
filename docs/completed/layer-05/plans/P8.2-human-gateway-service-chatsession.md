# Phase P8.2: HumanGatewayService ChatSession

**Parent**: Phase P8 (HumanGatewayService)  
**Spec References**: arch-v1.md L471 (HumanGatewayService), L726-736 (HumanGatewayService Chat), L462-474 (Hot-Reloadable Services)  
**Status**: ❌ PENDING

---

## Overview

- **Tests**: 4
- **Commits**: 4 (1:1 ratio)
- **Dependencies**: P8.1 (HumanGatewayService Core)
- **Files**: 1 (service.go modifications)

---

## Requirements

From `arch-v1.md L471` - HumanGatewayService:

> **L471**: `- **sys:human-gateway** — Chat interface for human-in-the-loop with running agents`

From `arch-v1.md L726-736` - HumanGatewayService Chat:

> **L726**: `System Service: sys:human-gateway`
> **L727**: 
> **L728**: `For any long-running top-level Agent, open an HTTPS chat session at /chat/{agentId}.`
> **L729**: 
> **L730**: `**Features**:`
> **L731**: `- Session receives read-only snapshot of Agent's current ContextMap + last N Messages (sanitized by boundary rules)`
> **L732**: `- Any message sent becomes mail_received (type: human_feedback or user) delivered to Agent's inbox`
> **L733**: `- Agent replies via normal mail → rendered back in chat UI`
> **L734**: `- Optional "action item" shorthand: @pause, @inject-memory X, etc. become special Mail messages`
> **L735**: 
> **L736**: `---`

From `arch-v1.md L462-474` - Hot-Reloadable Services:

> **L462**: `### 7.2 Hot-Reloadable Platform Services`
> **L463**: 
> **L464**: `All platform services are themselves Charts loaded by the ChartRegistry, running on the Statechart Engine.`

1. HTTPS chat session at /chat/{agentId}
2. Session receives read-only snapshot of ContextMap + last N Messages (sanitized by boundary rules)
3. Messages become mail_received (type: human_feedback or user)
4. Optional action item shorthand (@pause, @inject-memory X)

---

## Implementation Details

**Files to create/modify**:

- `pkg/services/humangateway/service.go` - Add chat session logic
- `pkg/services/humangateway/service_test.go` - Add chat tests

**Functions to implement**:

```go
func (s *HumanGatewayService) SendMessage(sessionID string, message Mail) error
func (s *HumanGatewayService) ParseActionItem(message string) (*Mail, error)
```

---

## Tests to Write (4 tests, 4 commits)

### Test 1: TestHumanGatewayService_SendMessage

```go
func TestHumanGatewayService_SendMessage(t *testing.T)
```

**Acceptance Criteria**:
- Message sent to agent inbox
- Message type set to human_feedback
- Message delivered via mail system
- Error returned for invalid session

**Commit Message**: `feat(layer-5/humangateway): send messages to agent inbox`

---

### Test 2: TestHumanGatewayService_ContextMapSanitization

```go
func TestHumanGatewayService_ContextMapSanitization(t *testing.T)
```

**Acceptance Criteria**:
- ContextMap sanitized by boundary rules
- Forbidden taints redacted
- Read-only snapshot provided
- Last N Messages included

**Commit Message**: `feat(layer-5/humangateway): sanitize ContextMap by boundary`

---

### Test 3: TestHumanGatewayService_ParseActionItem

```go
func TestHumanGatewayService_ParseActionItem(t *testing.T)
```

**Acceptance Criteria**:
- Action items parsed (@pause, @inject-memory)
- Special Mail messages created
- @pause creates pause command mail
- @inject-memory creates memory injection mail
- Unknown action items ignored

**Commit Message**: `feat(layer-5/humangateway): parse action item shorthands`

---

### Test 4: TestHumanGatewayService_AgentReply

```go
func TestHumanGatewayService_AgentReply(t *testing.T)
```

**Acceptance Criteria**:
- Agent reply rendered in chat UI
- Reply added to session messages
- Reply type marked as assistant
- Reply respects boundary sanitization

**Commit Message**: `feat(layer-5/humangateway): render agent replies in chat`

---

## Execution Order

```
P8.2: HumanGatewayService ChatSession
├── Test 1: TestHumanGatewayService_SendMessage
├── Test 2: TestHumanGatewayService_ContextMapSanitization
├── Test 3: TestHumanGatewayService_ParseActionItem
└── Test 4: TestHumanGatewayService_AgentReply
```

---

## TDD Workflow

1. `git checkout -b feat/layer5-humangateway-chat`
2. Stub `SendMessage`, `ParseActionItem` in service.go
3. Write `TestHumanGatewayService_SendMessage` → RED
4. Implement minimal SendMessage → GREEN
5. `git commit -m "feat(layer-5/humangateway): send messages to agent inbox"`
6. Repeat for tests 2-4

---

## Dependencies

- **P8.1**: HumanGatewayService Core must be complete

---

## Next Phase

After P8.2 completes, proceed to **P9.1: HotReloadProtocol Quiescence**
# Phase P4.2: HeartbeatAgentWake

**Parent**: Phase P4 (HeartbeatService)  
**Spec References**: arch-v1.md L469 (HeartbeatService HEARTBEAT.md), L462-474 (Hot-Reloadable Services)  
**Status**: ❌ PENDING

---

## Overview

| Metric | Value |
|--------|-------|
| Tests | 3 |
| Commits | 3 (1:1 ratio) |
| Dependencies | P4.1 |
| Branch | `feat/layer-05-heartbeat-agent-wake` |

---

## Requirements

From `arch-v1.md L469` - HeartbeatService HEARTBEAT.md:

> - **sys:heartbeat** — Scheduled agent wake-ups with HEARTBEAT.md injection

From `arch-v1.md L462-474` - Hot-Reloadable Services:

> All platform services SHALL be hot-reloadable without manual intervention

### Functional Requirements

1. Scheduled wake-ups inject HEARTBEAT.md into agent inbox
2. Wake-up triggers agent processing
3. Wake-up respects boundary rules (taints attached correctly)

---

## Implementation Details

### Files to Create/Modify

| File | Action |
|------|--------|
| `pkg/services/heartbeat/service.go` | Modify - Add wake-up logic |
| `pkg/services/heartbeat/service_test.go` | Modify - Add wake-up tests |

### Functions to Implement

```go
func (s *HeartbeatService) triggerWakeUp(agentID string) error
```

---

## Tests to Write (3 tests, 3 commits)

### Test 1: TestHeartbeatService_WakeAgent

```go
func TestHeartbeatService_WakeAgent(t *testing.T)
```

**Acceptance Criteria**:
- Agent receives HEARTBEAT.md injection
- Agent processing triggered
- Mail delivered to agent inbox

**Commit Message**: `feat(layer-05/heartbeat): TestHeartbeatService_WakeAgent`

---

### Test 2: TestHeartbeatService_WakeUpBoundaryEnforcement

```go
func TestHeartbeatService_WakeUpBoundaryEnforcement(t *testing.T)
```

**Acceptance Criteria**:
- HEARTBEAT.md respects boundary rules
- Taints attached correctly based on agent boundary
- Forbidden taints filtered out

**Commit Message**: `feat(layer-05/heartbeat): TestHeartbeatService_WakeUpBoundaryEnforcement`

---

### Test 3: TestHeartbeatService_WakeUpFailure

```go
func TestHeartbeatService_WakeUpFailure(t *testing.T)
```

**Acceptance Criteria**:
- Failed wake-up logged to observability
- Schedule continues for future wake-ups
- Error returned to caller

**Commit Message**: `feat(layer-05/heartbeat): TestHeartbeatService_WakeUpFailure`

---

## Execution Plan

1. `git checkout -b feat/layer-05-heartbeat-agent-wake`
2. Stub all public symbols (zero implementation, only signatures)
3. Write Test 1 (TestHeartbeatService_WakeAgent) → Run → RED → Implement → GREEN → Commit
4. Write Test 2 (TestHeartbeatService_WakeUpBoundaryEnforcement) → Run → RED → Implement → GREEN → Commit
5. Write Test 3 (TestHeartbeatService_WakeUpFailure) → Run → RED → Implement → GREEN → Commit
6. Run full test suite → Verify all green
7. Open PR

---

## Dependencies

- P4.1 (HeartbeatService Core) - Must be completed first

## Success Criteria

- All 3 tests pass
- 100% test coverage on modified files
- 3 commits with 1:1 test-to-commit ratio
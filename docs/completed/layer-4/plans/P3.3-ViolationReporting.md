# Phase P3.3: Violation Reporting

**Phase ID**: L4-P3.3  
**Title**: Violation Reporting via MailRouter  
**Parent**: Phase P3 (Boundary Enforcement)  
**Status**: ❌ PENDING

## Parent Requirements

From `arch-v1.md L286` - Taint Violation Event:
- Runtime guard: any action/guard can query taints; violation emits `taint_violation` event to dead-letter

From `arch-v1.md L170` - Mail Types:
- `taint_violation` is a valid Message type for reporting security violations

From `arch-v1.md L290-291` - Mail-only Transitions:
- All boundary enforcement goes through Mail system, enforced by `sys:security` System Service Chart

## Dependencies

- Phase P3.1 (Boundary Validation): ValidateBoundary for violation context
- Phase P3.2 (Boundary Transitions): EnforceTransition triggers violations

## Satisfied Lower-Layer Requirements

- Layer 3: Mail system with `MailTypeTaintViolation` type (arch-v1.md L170, pkg/mail/types.go L29, L42)
- Layer 2: sys:observability service for dead-letter queue (implementation-assessment.md L419-420)
- Layer 2: sys:security service skeleton (implementation-assessment.md L58-73)

## Acceptance Criteria

1. ReportViolation creates a `taint_violation` Mail with correct type
2. Violation Mail is sent to dead-letter queue (sys:observability)
3. Violation Mail includes all violation details (runtimeId, source, target, forbidden taints, timestamp)
4. Violation count is tracked and retrievable via ReportTaints

## Test Descriptions

### Test 1: TestReportViolation_CreateEvent

**Given**: A taint violation with runtimeId="agent-123", source=outer, target=inner, forbiddenTaints=["PII"]  
**When**: ReportViolation is called with the violation details  
**Then**: A Mail with Type=MailTypeTaintViolation is created  
**Expected Result**: Mail type is MailTypeTaintViolation, source is sys:security, target is sys:observability  
**Spec Reference**: arch-v1.md L286 (violation emits taint_violation event), L170 (Mail types)

### Test 2: TestReportViolation_DeadLetter

**Given**: A taint violation that needs to be reported  
**When**: ReportViolation is called and routes the violation Mail  
**Then**: Mail is sent to dead-letter queue via sys:observability  
**Expected Result**: sys:observability receives the taint_violation Mail for dead-letter processing  
**Spec Reference**: arch-v1.md L286 (to dead-letter), L459 (sys:observability service)

### Test 3: TestReportViolation_Details

**Given**: A taint violation with complete details including runtimeId, boundaries, taints  
**When**: ReportViolation is called with all violation details  
**Then**: All violation details are included in the Mail content  
**Expected Result**: Mail content contains runtimeId, source boundary, target boundary, forbidden taints, and timestamp  
**Spec Reference**: arch-v1.md L286 (violation details), L174 (Mail content field)

### Test 4: TestReportViolation_Count

**Given**: Multiple violations reported for the same runtimeId  
**When**: ReportViolation is called multiple times, then ReportTaints is called  
**Then**: Violation count is incremented and retrievable via ReportTaints  
**Expected Result**: ReportTaints returns TaintMap with violation count for the runtimeId  
**Spec Reference**: arch-v1.md L286 (Runtime guard can query taints), L1303 (ReportTaints interface)

## Implementation Plan

### Files to Modify

1. `pkg/security/violation.go` - Create violation reporting functions
2. `pkg/security/violation_test.go` - Add tests

### Stubs Needed

```go
// pkg/security/violation.go
type TaintViolation struct {
    RuntimeID       string
    SourceBoundary  BoundaryType
    TargetBoundary  BoundaryType
    ForbiddenTaints []string
    Timestamp       time.Time
}

type ViolationReport struct {
    Violations []TaintViolation
    Count      int
}

func ReportViolation(runtimeId string, violation TaintViolation) error
func GetViolationCount(runtimeId string) int
func createViolationMail(violation TaintViolation) Mail
```

### Implementation Order

1. Create `pkg/security/violation.go` with stub implementations
2. Write Test 1 → RED → Implement taint_violation Mail creation → GREEN → commit
3. Write Test 2 → RED → Implement dead-letter routing to sys:observability → GREEN → commit
4. Write Test 3 → RED → Implement violation details in Mail content → GREEN → commit
5. Write Test 4 → RED → Implement violation count tracking → GREEN → commit

## Commit Plan

| Test | Commit Message |
|------|----------------|
| Test 1 | `feat(layer-4/violation): add taint_violation Mail creation` |
| Test 2 | `feat(layer-4/violation): add dead-letter queue routing` |
| Test 3 | `feat(layer-4/violation): add violation details to Mail content` |
| Test 4 | `feat(layer-4/violation): add violation count tracking` |

## Deliverables

- 4 commits (1:1 test-to-commit ratio)
- All tests passing
- Files modified: `pkg/security/violation.go`, `pkg/security/violation_test.go`

---

*Document generated: 2026-03-09*  
*Based on: arch-v1.md v1.3, phase-breakdown.md, implementation-assessment.md*
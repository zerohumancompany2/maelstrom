# Phase P1.3: CheckViolation Implementation

**Parent**: Phase P1 (Taint Engine Core)  
**Gap References**: L4-H1 (CheckForbidden stubbed)  
**Status**: ❌ PENDING

## Overview
- Tests: 3
- Commits: 3 (1:1 ratio)
- Dependencies: P1.1-AttachTaint, P1.2-PropagateTaint

## SPEC

### Requirements
From `security-philosophy.md L109-110` - TaintEngine.CheckForbidden():
- Returns error if taints forbidden for boundary
- Validates taints allowed for boundary
- Enforces boundary crossing rules

### Implementation Details

**Files to create/modify**:
- `pkg/security/types.go` - Implement CheckForbidden enforcement logic
- `pkg/security/security_test.go` - Add 3 CheckViolation tests

**Functions to implement**:
```go
func (e *taintEngineImpl) CheckForbidden(taints []string, boundary BoundaryType) error
```

**Policy Rules**:
1. `INNER_ONLY` taint forbidden on DMZ and Outer boundaries
2. `PII` taint forbidden on Outer boundary
3. `SECRET` taint forbidden on DMZ and Outer boundaries
4. Empty taints or allowed taints pass without error

**Test scenarios**:
1. TestTaintEngine_CheckViolation_INNER_ONLY_on_DMZ - INNER_ONLY taint on DMZ boundary returns error
2. TestTaintEngine_CheckViolation_SECRET_on_Outer - SECRET taint on Outer boundary returns error
3. TestTaintEngine_CheckViolation_AllowedTaints - Allowed taints pass without error

## TDD Workflow

### Iteration 1: TestTaintEngine_CheckViolation_INNER_ONLY_on_DMZ
1. Write test: `TestTaintEngine_CheckViolation_INNER_ONLY_on_DMZ` in `pkg/security/security_test.go`
2. Run → RED (test fails because CheckForbidden returns nil)
3. Implement: Add INNER_ONLY forbidden check for DMZ/Outer boundaries
4. Run → GREEN
5. Commit: `feat(layer-4/taint-engine): CheckForbidden blocks INNER_ONLY on DMZ boundary`

### Iteration 2: TestTaintEngine_CheckViolation_SECRET_on_Outer
1. Write test: `TestTaintEngine_CheckViolation_SECRET_on_Outer` in `pkg/security/security_test.go`
2. Run → RED (test fails because SECRET not checked)
3. Implement: Add SECRET forbidden check for DMZ/Outer boundaries
4. Run → GREEN
5. Commit: `feat(layer-4/taint-engine): CheckForbidden blocks SECRET on Outer boundary`

### Iteration 3: TestTaintEngine_CheckViolation_AllowedTaints
1. Write test: `TestTaintEngine_CheckViolation_AllowedTaints` in `pkg/security/security_test.go`
2. Run → RED (test may fail or pass depending on implementation)
3. Implement: Ensure allowed taints pass without error
4. Run → GREEN
5. Commit: `feat(layer-4/taint-engine): CheckForbidden allows permitted taints`

## Deliverables
- 3 commits (one per test)
- All tests passing
- Files modified: `pkg/security/types.go`, `pkg/security/security_test.go`
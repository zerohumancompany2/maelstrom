# Phase P2.4: DataSource Registry Implementation

**Phase ID**: L4-P2.4  
**Title**: DataSource Registry Pattern  
**Parent**: Phase P2 (DataSource)  
**Status**: ⚠️ PARTIAL (registry exists, needs verification tests)

## Parent Requirements

From `arch-v1.md L787-793` - DataSource interface:
- Pluggable DataSource implementations
- Registry pattern for DataSource selection

From `implementation-assessment.md L88-93`:
1. Register DataSource implementations by name
2. Get DataSource by name
3. List registered DataSources

From `arch-v1.md L796-797` - Built-in and custom implementations:
- Built-ins: localDisk, s3, gcs, inMemoryWorkspace
- Roll-your-own: Register new implementation by name

## Dependencies

- P2.1 (LocalDisk) - localDisk registration
- P2.2 (S3) - s3 registration
- P2.3 (InMemory) - inMemory registration

## Satisfied Lower-Layer Requirements

- Layer 3: Mail system with `MailMetadata.Taints` field (arch-v1.md L170, L50)
- Layer 2: sys:security service skeleton exists (implementation-assessment.md L58-73)
- DataSource interface exists (implementation-assessment.md L88-93)
- Registry struct and methods exist (implementation-assessment.md L89-93)
- globalRegistry singleton exists (implementation-assessment.md L94)

## Acceptance Criteria

1. DataSource can be registered by name with factory function
2. Get returns registered DataSource instance when called with valid name
3. Get returns error for unknown DataSource name
4. List returns all registered DataSource names

## Test Descriptions

### Test 1: TestRegistry_Register

**Given**: A new DataSource Registry instance  
**When**: Register is called with name "custom" and a factory function  
**Then**: DataSource registered by name, subsequent Register with same name overwrites  
**Expected Result**: DataSource registered by name, no duplicate registration error (overwrite allowed)  
**Spec Reference**: arch-v1.md L797 (register new implementation), implementation-assessment.md L90

### Test 2: TestRegistry_Get

**Given**: A Registry with "localDisk" and "s3" DataSources registered  
**When**: Get is called with name "localDisk" and valid config, then with name "unknown"  
**Then**: Returns registered DataSource for valid name, returns nil/error for unknown name  
**Expected Result**: Returns registered DataSource, error/nil for unknown name  
**Spec Reference**: arch-v1.md L796-797 (built-in and custom implementations), implementation-assessment.md L91-92

### Test 3: TestRegistry_List

**Given**: A Registry with "localDisk", "s3", and "inMemory" DataSources registered  
**When**: List is called  
**Then**: Returns slice containing all three registered names  
**Expected Result**: Returns all registered names, includes built-in implementations  
**Spec Reference**: implementation-assessment.md L93 (List method), arch-v1.md L796 (built-ins)

## Implementation Plan

### Files to Modify

1. `pkg/datasource/datasource.go` - Verify registry implementation (already exists)
2. `pkg/datasource/datasource_test.go` - Add verification tests

### Existing Code (from implementation-assessment.md L88-94)

```go
// pkg/datasource/datasource.go - already implemented
type Registry struct {
    mu      sync.RWMutex
    sources map[string]func(map[string]any) (DataSource, error)
}

func (r *Registry) Register(name string, factory func(map[string]any) (DataSource, error))
func (r *Registry) Get(name string, config map[string]any) (DataSource, error)
func (r *Registry) List() []string
```

### Implementation Order

1. Review existing registry implementation in `pkg/datasource/datasource.go`
2. Write Test 1 → RED → Verify Register works correctly → GREEN → commit
3. Write Test 2 → RED → Verify Get returns correct DataSource or error → GREEN → commit
4. Write Test 3 → RED → Verify List returns all registered names → GREEN → commit

## Commit Plan

| Test | Commit Message |
|------|----------------|
| Test 1 | `feat(layer-4/datasource): verify Registry Register functionality` |
| Test 2 | `feat(layer-4/datasource): verify Registry Get functionality` |
| Test 3 | `feat(layer-4/datasource): verify Registry List functionality` |

## Deliverables

- 3 commits (1:1 test-to-commit ratio)
- All tests passing
- Files modified: `pkg/datasource/datasource_test.go` (existing registry code verified)

---

*Document generated: 2026-03-09*  
*Based on: arch-v1.md v1.3, phase-breakdown.md, implementation-assessment.md*
# Phase P6.6: allowedOnExit Sub-Agent Returns

**Phase ID**: L4-P6.6  
**Title**: allowedOnExit Enforcement on Sub-Agent Returns  
**Parent**: Phase P6 (allowedOnExit Enforcement)  
**Status**: ❌ PENDING

## Parent Requirements

From `arch-v1.md L280` - Data Leaving Runtime:
- On any data leaving the runtime (Mail, stream chunks, snapshot, sub-agent result) — uses allowedOnExit

From `arch-v1.md L1317` (SHALL statement):
- SHALL enforce allowedOnExit on Mail, streams, snapshots, sub-agent returns

From `arch-v1.md L1518` (enforcement point):
- Enforce allowedOnExit on all Mail, stream chunks, snapshots, and sub-agent returns

## Dependencies

- Phase P1: Taint Engine Core (AttachTaint, PropagateTaint)
- Phase P6.1: allowedOnExit Mail Enforcement
- Phase P6.2: allowedOnExit Stream Enforcement
- Phase P6.3: allowedOnExit Snapshot Enforcement
- Phase P6.5: allowedOnExit Policy Engine

## Satisfied Lower-Layer Requirements

- Layer 3: Sub-agent execution framework with result return mechanism (arch-v1.md L1317)
- Layer 2: sys:security service with allowedOnExit policy checking (implementation-assessment.md L58-73)
- Layer 0: ApplicationContext interface for policy lookups (arch-v1.md L383-387)

## Acceptance Criteria

1. Sub-agent return values are checked against allowedOnExit policy before being returned to parent agent
2. Tainted data in sub-agent results is blocked based on policy rules
3. Sub-agent return violations are logged and reported to parent agent
4. Clean data passes through allowedOnExit check without modification

## Test Descriptions

### Test 1: TestAllowedOnExit_SubAgentReturn_CleanDataPasses

**Given**: A sub-agent returns a result containing only clean (untainted) data  
**When**: allowedOnExit check is performed on the sub-agent return value  
**Then**: Data is returned to parent agent without modification or blocking  
**Expected Result**: Sub-agent result passes through, parent agent receives complete data  
**Spec Reference**: arch-v1.md L280 (data leaving runtime), L1317 (SHALL enforce on sub-agent returns)

### Test 2: TestAllowedOnExit_SubAgentReturn_TaintedDataBlocked

**Given**: A sub-agent returns a result containing tainted data (e.g., PII, SECRET)  
**When**: allowedOnExit check is performed and policy denies the taint types  
**Then**: Tainted data is blocked/replaced, violation is logged, parent receives sanitized result  
**Expected Result**: Tainted fields are removed/replaced with placeholders, violation logged to audit trail  
**Spec Reference**: arch-v1.md L280 (data leaving runtime), L1317 (SHALL enforce on sub-agent returns), L1518 (enforcement point)

## Implementation Plan

### Files to Modify

1. `pkg/security/allowed_on_exit.go` - Add sub-agent return enforcement
2. `pkg/security/allowed_on_exit_test.go` - Add tests
3. `pkg/orchestrator/subagent.go` - Integrate allowedOnExit check before return

### Stubs Needed

```go
// pkg/security/allowed_on_exit.go
func CheckSubAgentReturn(result any, policy *AllowedOnExitPolicy) (any, error)

// pkg/orchestrator/subagent.go
func (s *SubAgent) enforceAllowedOnExit(result any) (any, error)
```

### Implementation Order

1. Create stub functions in `pkg/security/allowed_on_exit.go` and `pkg/orchestrator/subagent.go`
2. Write Test 1 → RED → Implement clean data pass-through → GREEN → commit
3. Write Test 2 → RED → Implement tainted data blocking → GREEN → commit

## Commit Plan

| Test | Commit Message |
|------|----------------|
| Test 1 | `feat(layer-4/allowed-on-exit): add sub-agent return check for clean data` |
| Test 2 | `feat(layer-4/allowed-on-exit): add sub-agent return blocking for tainted data` |

## Deliverables

- 2 commits (1:1 test-to-commit ratio)
- All tests passing
- Files modified: `pkg/security/allowed_on_exit.go`, `pkg/security/allowed_on_exit_test.go`, `pkg/orchestrator/subagent.go`

---

*Document generated: 2026-03-09*  
*Based on: arch-v1.md v1.3, phase-breakdown.md, implementation-assessment.md*
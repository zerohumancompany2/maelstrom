# Phase P6.2: ToolRegistry BoundaryFiltering

**Parent**: Phase P6 (ToolRegistry)  
**Spec References**: arch-v1.md L472, L488 (ToolRegistry Boundary), L261-270 (Boundary Model)  
**Status**: ❌ PENDING

---

## Overview

- **Tests**: 3
- **Commits**: 3 (1:1 ratio)
- **Dependencies**: P6.1, Layer 4 (security package)
- **Files**: 1 (service.go modifications)

---

## Requirements

From `arch-v1.md L472, L488` - ToolRegistry Boundary:

> **L472**: `- **sys:tools** — Tool registry and resolution`
>
> **L488**: `- **sys:tools**: `resolve(name, callerBoundary) → ToolDescriptor | notFound``

From `arch-v1.md L261-270` - Boundary Model:

> **L261-270**: Boundary types (outer, DMZ, inner) and enforcement rules

1. Returns sanitized schema filtered by caller's boundary
2. Inner tools are invisible to outer callers
3. Boundary enforcement on tool resolution

---

## Implementation Details

**Files to create/modify**:

- `pkg/services/tools/service.go` - Add boundary filtering logic
- `pkg/services/tools/service_test.go` - Add boundary tests

**Functions to implement**:

```go
func (r *ToolRegistry) filterByBoundary(tools []ToolDescriptor, boundary BoundaryType) []ToolDescriptor
func (r *ToolRegistry) sanitizeSchema(tool ToolDescriptor, boundary BoundaryType) ToolDescriptor
```

---

## Tests to Write (3 tests, 3 commits)

### Test 1: TestToolRegistry_BoundaryFiltering

```go
func TestToolRegistry_BoundaryFiltering(t *testing.T)
```

**Acceptance Criteria**:
- Tools filtered by caller boundary
- Schema sanitized for boundary
- Outer callers see all tools
- DMZ callers see DMZ and outer tools
- Inner callers see all tools

**Commit Message**: `feat(layer-5/tools): filter tools by caller boundary`

---

### Test 2: TestToolRegistry_InnerToolsInvisible

```go
func TestToolRegistry_InnerToolsInvisible(t *testing.T)
```

**Acceptance Criteria**:
- Inner boundary tools hidden from outer callers
- DMZ tools visible to DMZ and outer callers
- Inner tools visible only to inner callers
- Boundary violations logged

**Commit Message**: `feat(layer-5/tools): hide inner tools from outer callers`

---

### Test 3: TestToolRegistry_BoundaryEnforcement

```go
func TestToolRegistry_BoundaryEnforcement(t *testing.T)
```

**Acceptance Criteria**:
- Boundary violation logged to observability
- Tool resolution fails for forbidden boundaries
- Error includes boundary type and tool name
- Audit trail created for violations

**Commit Message**: `feat(layer-5/tools): enforce boundary on tool resolution`

---

## Execution Order

```
P6.2: ToolRegistry BoundaryFiltering
├── Test 1: TestToolRegistry_BoundaryFiltering
├── Test 2: TestToolRegistry_InnerToolsInvisible
└── Test 3: TestToolRegistry_BoundaryEnforcement
```

---

## TDD Workflow

1. `git checkout -b feat/layer5-toolregistry-boundary`
2. Stub `filterByBoundary`, `sanitizeSchema` in service.go
3. Write `TestToolRegistry_BoundaryFiltering` → RED
4. Implement minimal boundary filtering → GREEN
5. `git commit -m "feat(layer-5/tools): filter tools by caller boundary"`
6. Repeat for tests 2-3

---

## Dependencies

- **P6.1**: ToolRegistry Core must be complete
- **Layer 4**: Security package with BoundaryType and enforcement

---

## Next Phase

After P6.2 completes, proceed to **P7.1: DataSourceService Core**
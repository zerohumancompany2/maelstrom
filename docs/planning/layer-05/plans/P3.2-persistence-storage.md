# Phase P3.2: PersistenceStorageBackend

**Phase ID**: L5-P3.2  
**Title**: PersistenceStorageBackend  
**Parent**: Phase P3 (PersistenceService)  
**Status**: ❌ PENDING

## Parent Requirements

From `arch-v1.md L468, L486` - PersistenceService Storage:
1. StorageBackend must save snapshots
2. StorageBackend must load snapshots
3. StorageBackend must save events
4. StorageBackend must query events

From `arch-v1.md L462-474` (Hot-Reloadable Services):
- Platform services SHALL be hot-reloadable without manual intervention

## Dependencies

- P3.1: PersistenceService Core (StorageBackend interface defined)

## Satisfied Lower-Layer Requirements

- Layer 4: Security infrastructure complete (taint engine, boundary enforcement)
- Layer 3: Mail system complete (publish/subscribe, routing, inbox)
- Layer 2: Service registry complete (Service interface, ServiceRegistry)
- Layer 1: ChartRegistry exists (YAML loading infrastructure)
- Layer 0: Statechart Engine complete

## Acceptance Criteria

1. StorageBackend can save snapshots with unique IDs
2. StorageBackend can load snapshots correctly with all data preserved
3. StorageBackend can save events with timestamps associated with runtime ID
4. StorageBackend can query events by runtime ID and filter by time range

## Test Descriptions

### Test 1: TestStorageBackend_SaveSnapshot

**Given**: A StorageBackend instance and a snapshot to save  
**When**: SaveSnapshot() is called with snapshot  
**Then**: Snapshot saved with unique ID  
**Expected Result**: Snapshot saved successfully, retrievable by ID  
**Spec Reference**: arch-v1.md L468 (Snapshots), L486 (snapshot(runtimeId))

### Test 2: TestStorageBackend_LoadSnapshot

**Given**: A StorageBackend instance with a previously saved snapshot  
**When**: LoadSnapshot() is called with snapshot ID  
**Then**: Snapshot loaded correctly with all data preserved  
**Expected Result**: All snapshot data preserved, runtime state intact, taints included  
**Spec Reference**: arch-v1.md L468 (Snapshots), L486 (restore(snapshotId))

### Test 3: TestStorageBackend_SaveEvent

**Given**: A StorageBackend instance and an event to save  
**When**: SaveEvent() is called with event  
**Then**: Event saved with timestamp and associated with runtime ID  
**Expected Result**: Event persisted with timestamp, runtime ID association maintained  
**Spec Reference**: arch-v1.md L468 (event sourcing)

### Test 4: TestStorageBackend_QueryEvents

**Given**: A StorageBackend instance with events saved  
**When**: QueryEvents() is called with filters  
**Then**: Events returned filtered by runtime ID and time range  
**Expected Result**: Events filtered correctly by runtime ID, time range filtering functional  
**Spec Reference**: arch-v1.md L468 (event sourcing)

## Implementation Plan

### Files to Modify

1. `pkg/services/persistence/storage.go` - StorageBackend interface and in-memory implementation
2. `pkg/services/persistence/storage_test.go` - Add tests

### Stubs Needed

```go
// pkg/services/persistence/storage.go
type StorageBackend interface {
    SaveSnapshot(snapshot Snapshot) error
    LoadSnapshot(id string) (Snapshot, error)
    SaveEvent(event Event) error
    QueryEvents(filters EventFilters) ([]Event, error)
}
```

### Implementation Order

1. Create `pkg/services/persistence/storage.go` with StorageBackend interface and in-memory implementation
2. Write Test 1 → RED → Implement SaveSnapshot → GREEN → commit
3. Write Test 2 → RED → Implement LoadSnapshot → GREEN → commit
4. Write Test 3 → RED → Implement SaveEvent → GREEN → commit
5. Write Test 4 → RED → Implement QueryEvents → GREEN → commit

## Commit Plan

| Test | Commit Message |
|------|----------------|
| Test 1 | `feat(layer-5/persistence): add StorageBackend SaveSnapshot` |
| Test 2 | `feat(layer-5/persistence): add StorageBackend LoadSnapshot` |
| Test 3 | `feat(layer-5/persistence): add StorageBackend SaveEvent` |
| Test 4 | `feat(layer-5/persistence): add StorageBackend QueryEvents` |

## Deliverables

- 4 commits (1:1 test-to-commit ratio)
- All tests passing
- Files modified: `pkg/services/persistence/storage.go`, `pkg/services/persistence/storage_test.go`

---

*Document generated: 2026-03-09*  
*Based on: arch-v1.md v1.3, phase-breakdown.md, implementation-assessment.md*
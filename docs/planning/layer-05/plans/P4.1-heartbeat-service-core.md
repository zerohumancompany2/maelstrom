# Phase P4.1: HeartbeatService Core

**Parent**: Phase P4 (HeartbeatService)  
**Spec References**: arch-v1.md L469 (HeartbeatService), L462-474 (Hot-Reloadable Services)  
**Status**: ❌ PENDING

---

## Overview

| Metric | Value |
|--------|-------|
| Tests | 3 |
| Commits | 3 (1:1 ratio) |
| Dependencies | None |
| Branch | `feat/layer-05-heartbeat-core` |

---

## Requirements

From `arch-v1.md L469` - HeartbeatService:

> - **sys:heartbeat** — Scheduled agent wake-ups with HEARTBEAT.md injection

From `arch-v1.md L462-474` - Hot-Reloadable Services:

> All platform services SHALL be hot-reloadable without manual intervention

### Functional Requirements

1. HeartbeatService must return ID "sys:heartbeat"
2. HeartbeatService must schedule wake-ups using cron expressions
3. HeartbeatService must unschedule wake-ups by job ID

---

## Implementation Details

### Files to Create/Modify

| File | Action |
|------|--------|
| `pkg/services/heartbeat/service.go` | Modify - Complete HeartbeatService implementation |
| `pkg/services/heartbeat/service_test.go` | Modify - Add tests |

### Functions to Implement

```go
func NewHeartbeatService() *HeartbeatService
func (s *HeartbeatService) ID() string
func (s *HeartbeatService) HandleMail(mail Mail) error
func (s *HeartbeatService) ScheduleAgentWake(agentID string, cron string) error
func (s *HeartbeatService) Unschedule(jobID string) error
```

---

## Tests to Write (3 tests, 3 commits)

### Test 1: TestHeartbeatService_ID

```go
func TestHeartbeatService_ID(t *testing.T)
```

**Acceptance Criteria**:
- HeartbeatService.ID() returns "sys:heartbeat"
- ID is immutable after construction

**Commit Message**: `feat(layer-05/heartbeat): TestHeartbeatService_ID`

---

### Test 2: TestHeartbeatService_Schedule

```go
func TestHeartbeatService_Schedule(t *testing.T)
```

**Acceptance Criteria**:
- Cron schedule parsed correctly
- Job registered with scheduler
- Job ID returned for later unscheduling

**Commit Message**: `feat(layer-05/heartbeat): TestHeartbeatService_Schedule`

---

### Test 3: TestHeartbeatService_Unschedule

```go
func TestHeartbeatService_Unschedule(t *testing.T)
```

**Acceptance Criteria**:
- Job removed from scheduler
- No more wake-ups for agent
- Unschedule of non-existent job returns error

**Commit Message**: `feat(layer-05/heartbeat): TestHeartbeatService_Unschedule`

---

## Execution Plan

1. `git checkout -b feat/layer-05-heartbeat-core`
2. Stub all public symbols (zero implementation, only signatures)
3. Write Test 1 (TestHeartbeatService_ID) → Run → RED → Implement → GREEN → Commit
4. Write Test 2 (TestHeartbeatService_Schedule) → Run → RED → Implement → GREEN → Commit
5. Write Test 3 (TestHeartbeatService_Unschedule) → Run → RED → Implement → GREEN → Commit
6. Run full test suite → Verify all green
7. Open PR

---

## Dependencies

- None (can start immediately)

## Success Criteria

- All 3 tests pass
- 100% test coverage on modified files
- 3 commits with 1:1 test-to-commit ratio
# Phase P10.1: PlatformServiceYAML Schema

**Parent**: Phase P10 (Integration)  
**Spec References**: arch-v1.md L1110-1128 (PlatformService YAML Schema), L462-474 (Hot-Reloadable Services)  
**Status**: ❌ PENDING

## Overview

- Tests: 3
- Commits: 3 (1:1 ratio)
- Dependencies: All previous phases (P1-P9)

## Requirements

From `arch-v1.md L1110-1128` - PlatformService YAML Schema:

```yaml
### 13.7 PlatformService

apiVersion: maelstrom.dev/v1
kind: PlatformService
metadata:
  name: sys:gateway
  core: true                    # treated specially by Registry
spec:
  chartRef: gateway-v1
  requiredForKernelReady: true
  replicas: 1
  persistence:
    snapshotEvery: "100 messages"
  expose:
    http:
      port: 8080
      tls: true
```

1. PlatformService YAML schema defined (L1112-1128)
2. ChartRegistry loads PlatformService YAMLs (L1116-1117)
3. Services marked as core: true treated specially (L1117)

From `arch-v1.md L462-474` - Hot-Reloadable Services:
- All platform services are hot-reloadable without manual intervention

## Implementation Details

**Files to create/modify**:

- `charts/platform-services/gateway.yaml` - Gateway PlatformService YAML
- `charts/platform-services/*.yaml` - All other service YAMLs (admin, persistence, heartbeat, memory, tools, datasources, humangateway)

**YAML Schema Requirements**:

```yaml
apiVersion: maelstrom.dev/v1
kind: PlatformService
metadata:
  name: sys:<service-name>
  core: true
spec:
  chartRef: <chart-reference>
  requiredForKernelReady: true
  replicas: 1
```

## Tests to Write (3 tests, 3 commits)

### Test 1: TestPlatformServiceYAML_Schema

```go
func TestPlatformServiceYAML_Schema(t *testing.T)
```

**Acceptance Criteria**:
- YAML schema validated against spec (L1112-1128)
- Required fields present: apiVersion, kind, metadata.name, metadata.core, spec.chartRef
- Optional fields supported: spec.requiredForKernelReady, spec.replicas, spec.persistence, spec.expose

### Test 2: TestPlatformServiceYAML_ChartRegistryLoad

```go
func TestPlatformServiceYAML_ChartRegistryLoad(t *testing.T)
```

**Acceptance Criteria**:
- ChartRegistry loads PlatformService YAMLs from charts/platform-services/
- Services registered correctly with sys:* IDs
- YAML parsing succeeds for all 8 platform services

### Test 3: TestPlatformServiceYAML_CoreServices

```go
func TestPlatformServiceYAML_CoreServices(t *testing.T)
```

**Acceptance Criteria**:
- Core services (metadata.core: true) treated specially by Registry (L1117)
- requiredForKernelReady enforced - kernel cannot start without core services
- Core services loaded before user agent charts

## Commit Plan

| Commit | Test | Description |
|--------|------|-------------|
| 1 | TestPlatformServiceYAML_Schema | feat(layer-5/platform-yaml): validate PlatformService YAML schema |
| 2 | TestPlatformServiceYAML_ChartRegistryLoad | feat(layer-5/platform-yaml): ChartRegistry loads PlatformService YAMLs |
| 3 | TestPlatformServiceYAML_CoreServices | feat(layer-5/platform-yaml): core services treated specially |